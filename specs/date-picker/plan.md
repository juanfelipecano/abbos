# Plan: date picker

1. Design: update `Date Picker.dc.html` (format, triggers, placement, single and range). Done.
2. `date-utils.ts`, `date-picker.types.ts`, `date-picker.tokens.ts` with unit tests. Done.
3. `AbCalendar` (`calendar/`). Done.
4. `AbDatePicker` and `AbDatePickerTrigger`. Done.
5. Export from `components/index.ts`. Done.
6. Playground page `features/components/date-picker/` and registry entry. Done.
7. Verify: `ng build abbos`, `ng test`, `ng build playground`, Prettier on touched paths. Done.
